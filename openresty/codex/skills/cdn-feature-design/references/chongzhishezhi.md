# Recharge / Payment Settings

## Scope
- Configure payment gateways (WeChat/Alipay or equivalents).
- Set callback URLs and verification keys.

## UI
- Form inputs for app ID, merchant ID, key/secret, and notify URLs.
- Test and enable/disable gateways.

## API
- Save/load gateway config; validate required fields.
- Provide callback endpoints for payment notifications.
