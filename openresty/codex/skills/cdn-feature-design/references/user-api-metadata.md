# User API Metadata

## Scope
- Map user-facing routes to backend endpoints.
- Track which APIs are required for user pages and which are admin-only.

## Notes
- User APIs should not expose admin-only fields.
- Missing endpoints should be implemented with user scoping.
