# Global Configuration

## Scope
- System-wide defaults used when user/site settings are absent.
- Nginx config, log paths, worker settings, and feature toggles.

## UI
- Global configuration editor; admin-only.

## API
- Fetch/save global configs and config items.

## Node/Sync
- Sync global config changes to nodes and reload safely.
