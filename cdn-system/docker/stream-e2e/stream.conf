# Generated to mirror agent renderStreamConfig output for stream-e2e.
upstream stream_up_tcp {
    server origin:9000;
}

server {
    listen 19001;
    proxy_pass stream_up_tcp;
    proxy_connect_timeout 10s;
    proxy_timeout 60s;
}

upstream stream_up_udp_udp {
    server origin:9001;
}

server {
    listen 19002 udp;
    proxy_pass stream_up_udp_udp;
    proxy_connect_timeout 10s;
    proxy_timeout 60s;
}
